{
  "buildCommand": "cd portal && npm install --legacy-peer-deps && CI=false npm run build",
  "outputDirectory": "portal/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
